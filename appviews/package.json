{
  "name": "@gertrude/webviews",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@shared/tailwind": "workspace:*",
    "preact": "^10.11.3",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "devDependencies": {
    "classnames": "2.3.2"
  }
}
