import React from 'react';

type Props = {
  className?: string;
};

const ActivityOverview: React.FC<Props> = ({ className }) => (
  <h1 className={className}>ActivityOverview placeholder</h1>
);

export default ActivityOverview;
